import React, { FC, useState } from 'react'
import PostOverView from '../../components/organisms/PostOverView'

const PostOverviewContainer: FC = () => {
  return <PostOverView />
}

export default PostOverviewContainer
