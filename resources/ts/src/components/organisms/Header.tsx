import React, { FC } from 'react'
import { colors } from '../../utilities/colors'
import NaviIcon from '../atoms/svg/NaviIcon'

// type Props = {}

const Header: FC = () => {
  return (
    <div>
      <NaviIcon color={colors.lightGray} />
      <p>aaaaa</p>
    </div>
  )
}

export default Header
